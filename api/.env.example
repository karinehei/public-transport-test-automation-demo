# Ticketing API – environment variables
#
# Local development: no variables required.
# Run with: uvicorn app:app --host 0.0.0.0 --port 8000
#
# Render: PORT is set automatically. No configuration needed.
